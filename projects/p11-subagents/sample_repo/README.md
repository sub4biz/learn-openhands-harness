# urlshort

A tiny URL shortener.

## Commands

- `python app.py shorten <url>` returns a short code.
- `python app.py resolve <code>` returns the original URL.
- `python app.py stats` prints how many URLs are stored.

## Tests

Run the suite with `pytest -q`.
