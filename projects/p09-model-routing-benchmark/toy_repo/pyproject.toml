[project]
name = "toyapp"
version = "0.1.0"
description = "Small repo used by the model routing lesson."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
toyapp = "toyapp.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
